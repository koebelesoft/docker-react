language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t koebelesoft/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true koebelesoft/docker-react npm run test